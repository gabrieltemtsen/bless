/**
 * @deprecated The big hero "trust the dev" banner was removed in favour of
 * a quieter sidebar credit. See `@/components/wallet/BackTheDev` instead.
 *
 * This file is kept as a tombstone so that git history is easy to follow;
 * the export below is a no-op so any stray import won't break the build.
 */
export function TrustCreatorCard() {
  return null;
}
